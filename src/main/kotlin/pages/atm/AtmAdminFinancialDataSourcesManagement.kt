package pages.atm

import org.openqa.selenium.WebDriver
import pages.core.annotations.PageUrl

@PageUrl("/financial-sources")
class AtmAdminFinancialDataSourcesManagement(driver: WebDriver) : AtmAdminPage(driver) {
}